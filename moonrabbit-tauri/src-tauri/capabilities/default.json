{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Moonrabbit 桌面端默认能力（WebView 加载 localhost + 基础窗口）",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default"
  ]
}
